<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:sec="http://www.springframework.org/schema/security" xmlns:task="http://www.springframework.org/schema/task"
	xmlns:tx="http://www.springframework.org/schema/tx" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="     http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd     http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd     http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd      http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-3.0.xsd     http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-3.0.xsd      http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.2.xsd      http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd">
	<!-- Enable component scanning for defining beans with annotations. -->
	<context:component-scan base-package="es.enxenio.ilga.pdmapping">
		<context:exclude-filter expression="org.springframework.stereotype.Controller"
			type="annotation" />
	</context:component-scan>

	<context:property-placeholder location="classpath:jdbc.properties"
		ignore-unresolvable="true" />

	<!-- Data source -->
	<bean class="com.mchange.v2.c3p0.ComboPooledDataSource"
		destroy-method="close" id="dataSource">
		<property name="driverClass">
			<value>${datasource.driverClass}</value>
		</property>
		<property name="jdbcUrl">
			<value>${datasource.jdbcUrl}</value>
		</property>
		<property name="properties">
			<props>
				<prop key="c3p0.min_size">${datasource.c3p0.min_size}</prop>
				<prop key="c3p0.max_size">${datasource.c3p0.max_size}</prop>
				<prop key="c3p0.acquire_increment">${datasource.c3p0.acquire_increment}</prop>
				<prop key="c3p0.idle_test_period">${datasource.c3p0.idle_test_period}</prop>
				<prop key="c3p0.max_statements">${datasource.c3p0.max_statements}</prop>
				<prop key="c3p0.maxConnectionAge">${datasource.c3p0.maxConnectionAge}</prop>
				<prop key="c3p0.maxIdleTime">${datasource.c3p0.maxIdleTime}</prop>
				<prop key="c3p0.preferredTestQuery">${datasource.c3p0.preferredTestQuery}</prop>
				<prop key="user">${datasource.user}</prop>
				<prop key="password">${datasource.password}</prop>
			</props>
		</property>
	</bean>
	<!-- Configuración para JNDI (la configuración de Jetty en main/jetty/jetty-env.xml, 
		para Tomcat habría que configurarlo). Por C3P0 no hace falta configurar nada 
		en Tomcat. -->
	<!-- <bean class="org.springframework.jndi.JndiObjectFactoryBean" id="dataSource" 
		p:jndiName="${datasource.jndiName}" p:resourceRef="true" /> -->
	<!-- LazyConnection Proxy (can be removed) -->

	<bean
		class="org.springframework.jdbc.datasource.LazyConnectionDataSourceProxy"
		id="dataSourceProxy" p:targetDataSource-ref="dataSource" />

	<!-- Hibernate Session Factory -->
	<bean class="org.springframework.orm.hibernate4.LocalSessionFactoryBean"
		id="sessionFactory" p:configLocation="classpath:/hibernate-config.xml"
		p:dataSource-ref="dataSourceProxy">
		<property name="packagesToScan" value="es.enxenio.ilga.pdmapping.modelo.entidades" />
	</bean>

	<!-- Transaction manager for a single Hibernate SessionFactory. -->
	<bean class="org.springframework.orm.hibernate4.HibernateTransactionManager"
		id="transactionManager" p:sessionFactory-ref="sessionFactory" />

	<tx:annotation-driven transaction-manager="transactionManager" />
</beans>